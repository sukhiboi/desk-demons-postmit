extends components/profilePage.pug

append posts
  .categories
    a.category.active-category(href=`/user/${username}`) Posts
    a.category(href='#') Likes
  .feed
    .posts
        include components/post.pug
        each item in likedPosts 
            +post(item)